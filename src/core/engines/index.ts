// core/engines — Strategy Pattern
// Each game variant (Dixit, Stella, …) will be implemented as a GameEngine here.
// A GameEngine receives a pure game state and returns the next state + events to emit.

export interface GameEngine {
  // to be defined
}
